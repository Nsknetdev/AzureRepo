name: Trigger auto deployment for azuretestweatherforecastapp

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ master ]
    paths:
    - '**'
    - '.github/workflows/azuretestweatherforecastapp-AutoDeployTrigger-50c7d0ce-1f43-44b1-b611-811cbf4e15b2.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.AZURETESTWEATHERFORECASTAPP_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURETESTWEATHERFORECASTAPP_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURETESTWEATHERFORECASTAPP_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          registryUrl: 
          registryUsername: ${{ secrets.AZURETESTWEATHERFORECASTAPP_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.AZURETESTWEATHERFORECASTAPP_REGISTRY_PASSWORD }}
          containerAppName: azuretestweatherforecastapp
          resourceGroup: AzureLearn
          imageToBuild: default/[parameters('containerAppName')]:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


